#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../bunyan/bin/bunyan" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../bunyan/bin/bunyan" "$@"
  ret=$?
fi
exit $ret
